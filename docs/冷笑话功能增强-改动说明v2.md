# Punchliner 冷笑话功能增强 - 改动说明 v2

## 一、需求回顾

### 用户需求
1. **今日冷笑话** - 放在首页最上方，暗调风格，缓存一天，换一个按钮
2. **冷笑话列表** - 改为20条，放详情页（点击类似冷笑话按钮时展示）
3. **详情页风格** - 和段子详情页保持一致（亮色调，不要暗色调）
4. **图片风格** - 黑色圆形背景 + 可爱猫咪 + 文字下方居中
5. **分格逻辑** - 按句子拆分，每句一个宫格
6. **详情页按钮** - 两个：图片生成、类似冷笑话

---

## 二、本次修改内容

### 1. 首页调整
- 移除冷笑话列表（不再在首页展示）
- 保留：今日冷笑话 + 今日笑一笑 + 更多段子

### 2. 冷笑话详情页
- **风格**：改为亮色调（与段子详情页一致）
- **断句逻辑**：智能拆分句子，每句一个宫格
- **按钮**：生成配图 + 类似冷笑话

### 3. 批量生成
- 从10条改为20条

---

## 三、文件改动清单

### 修改的文件

| 文件路径 | 改动内容 |
|---------|---------|
| `src/app/page.tsx` | 移除冷笑话列表相关代码 |
| `src/app/cold-joke/[id]/page.tsx` | 重写：亮色调 + 类似冷笑话功能 + 智能断句 |
| `src/app/cold-joke/[id]/page.module.css` | 重写：亮色调样式 |
| `src/app/api/jokes/cold/list/route.ts` | 批量生成改为20条 |

### 新增的文件

| 文件路径 | 说明 |
|---------|------|
| `src/app/api/ai/generate-similar-cold/route.ts` | 类似冷笑话生成API |

---

## 四、断句逻辑

```javascript
// 智能断句函数
function splitIntoSentences(content: string): string[] {
  // 按句末符号分割：。！？；
  const sentences = content
    .split(/([。！？；]+)/)
    .filter((s) => s.trim().length > 0)
  
  // 重新组合句子和标点
  const result: string[] = []
  for (let i = 0; i < sentences.length; i += 2) {
    const sentence = sentences[i] || ''
    const punctuation = sentences[i + 1] || ''
    if (sentence.trim()) {
      result.push(sentence.trim() + punctuation)
    }
  }
  
  return result
}
```

**示例**：
- 输入："我问树，树为什么不会说话，树说，因为我没下载语音包。"
- 输出：["我问树，树为什么不会说话，", "树说，因为我没下载语音包。"]
- 宫格数：2

---

## 五、页面结构

```
首页 /
├── 今日冷笑话 ❄️（暗调卡片）
│   ├── 内容
│   └── 换一个按钮
├── 今日笑一笑 ✨（亮调卡片）
│   ├── 内容
│   └── 换一个按钮
└── 更多段子 🎭
    └── 卡片列表

冷笑话详情页 /cold-joke/[id]（亮色调）
├── 内容展示 + 宫格数
├── [生成配图] 按钮
└── [类似冷笑话] 按钮

弹窗
├── 生成配图：多宫格展示（每句一张）
└── 类似冷笑话：列表展示，点击跳转
```

---

## 六、待确认项

- [ ] 断句逻辑是否符合预期？
- [ ] 图片宫格展示效果（纵向排列还是网格？）
- [ ] 类似冷笑话生成5条是否足够？
